# -*- coding: utf-8 -*-
"""it-spirit {{{ theme.name }}} Theme."""

from {{{ package.dottedname }}} import config
from zope.i18nmessageid import MessageFactory

import logging


logger = logging.getLogger(config.PROJECT_NAME)
_ = MessageFactory('{{{ package.dottedname }}}')
